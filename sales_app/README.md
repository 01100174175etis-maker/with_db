"""Sales App
Modern GUI for recording customer sales (name, item, quantity, date).
Stores data in a local SQLite database and supports export to Excel.

Run:
  python -m sales_app.main

Requirements:
  - Python 3.8+
  - ttkbootstrap
  - tkcalendar
  - pandas
  - openpyxl
"""
